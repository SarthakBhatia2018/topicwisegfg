#include <iostream>
#include <chrono>
#include <vector>

using namespace std;

int ran()
{
    return int(rand(0, 1000));
}

int main()
{
    vector<int> v(1000);
    // f = []() return (int)rand(1000);
    return 0;
}